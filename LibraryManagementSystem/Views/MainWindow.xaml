<Window x:Class="LibraryManagementSystem.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem.Views"
        mc:Ignorable="d"
        Title="Library Management System" Height="500" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Lista Cărți Disponibile" 
                   FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,15"/>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  BorderBrush="LightGray" BorderThickness="1">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding BookId}" Width="50"/>
                <DataGridTextColumn Header="Titlu" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Autor" Binding="{Binding Author}" Width="150"/>

                <DataGridTextColumn Header="Categorie" Binding="{Binding CategoryName}" Width="100"/>

                <DataGridTextColumn Header="Stoc" Binding="{Binding CurrentStock}" Width="60"/>

                <DataGridCheckBoxColumn Header="Disponibil" 
                        Binding="{Binding IsAvailable, Mode=OneWay}" 
                        Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">

            <Button Content="Șterge Cartea Selectată"
                    Command="{Binding DeleteCommand}"
                    Background="#FFDDDD" Foreground="Red" BorderBrush="Red"
                    Padding="15,5" Margin="10,0,0,0" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>