<UserControl x:Class="LibraryManagementSystem.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid>
        <Border Background="#F8F9FA" Width="400" VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="15" Padding="40">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Autentificare" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Email" FontWeight="SemiBold" Foreground="{StaticResource TextColor}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Height="40" Margin="0,0,0,15"/>

                <TextBlock Text="Parolă" FontWeight="SemiBold" Foreground="{StaticResource TextColor}" Margin="0,0,0,5"/>
                <PasswordBox x:Name="UserPasswordBox" Style="{StaticResource ModernPasswordBox}" Height="40" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorColor}" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,0,0,15" FontWeight="Bold"/>

                <Button Content="LOGIN" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=UserPasswordBox}" Style="{StaticResource PrimaryButton}" Height="45"/>

                <Button Content="Nu ai cont? Creează unul nou" Command="{Binding GoToRegisterCommand}" Margin="0,20,0,0" Background="Transparent" BorderThickness="0" Foreground="{StaticResource PrimaryColor}" Cursor="Hand" HorizontalAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline" Foreground="{TemplateBinding Foreground}"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>